# define CAR_H
# define CAT_H
# include "./../../../incl/config.h"

signed char *(__cdecl cv_tz_utc(signed char(*argp))) {
auto signed char c[] = ("+");
auto signed char a[] = ("UTC");
if(!argp) return(argp);
if(!(cmp(c,argp))) return(cathy(a));
return(catta(a,argp));
}
