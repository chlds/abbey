# define CAR_H
# include "./incl/config.h"

signed char *(__cdecl cath_alphanum(signed char(*argp))) {
auto signed char *b;
auto signed r;
b = cat_alphanum(argp);
r = cue_noalphanum(b);
if(none(r)) return(b);
bury(r+(b));
return(cathy_xe(b));
}
