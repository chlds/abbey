# define CAR_H
# include "./incl/config.h"

signed(__cdecl cuet_r(signed char(*argp))) {
auto signed r;
r = cueht(argp);
if(r) return(r);
r = cuelf(argp);
if(r) return(r);
r = cuecr(argp);
if(r) return(r);
return(cuet_rr(argp));
}
