# define CAR_H
# include "./incl/config.h"

signed char *(__cdecl catt(signed(arg))) {
auto signed char *b;
auto signed r;
AND(r,0x00);
b = (0x00);
r++;
r++;
r = (r*(sizeof(*b)));
b = (signed char*)(alloc(r));
if(!b) return(b);
*b = (0x00);
return(catt_r(arg,b));
}
