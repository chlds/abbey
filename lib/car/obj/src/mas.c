/* Synopsis

Mask for data type char
*/


# define CAR_H
# include "./incl/config.h"

signed(__cdecl mas(void)) {
auto signed r;
auto signed m = (0xFF);
r = r_pow(0x02,bpb());
--r;
if(!(0x00<(r))) r = (m);
return(r);
}
