/* Synopsis

Convert into a double word
*/


# define CAR_H
# include "./incl/config.h"

signed(__cdecl cnv(signed char(*args),signed(argp))) {
auto signed r;
auto signed char a[] = ("-");
auto signed(__cdecl *b)(signed(arg));
auto signed(__cdecl **w)(signed(arg));
auto signed(__cdecl *(v[]))(signed(arg)) = {
neg,
0x00,
};
AND(r,0x00);
if(!args) return(r);
w = (v);
if(cmp_part(a,args)) w++;
b = (*w);
r = cnv_r(args,argp);
if(b) r = b(r);
return(r);
}
