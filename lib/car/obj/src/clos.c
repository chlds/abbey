# define CAR_H
# define LIMITS_H
# include "./../../../incl/config.h"

signed(__cdecl clos(signed(arg))) {
auto signed r;
r = (CHAR_BIT*(sizeof(signed short)));
SHL(arg,r);
return(r+(0x01+(~clo_r(arg,r))));
}
